version: '3.7'
services: 
        samba_server:
                build: .        
                ports :
                        - "137:137/udp"
                        - "138:138/udp"
                        - "139:139"
                        - "445:445"
                volumes:
                        - type: volume
                          source: samba
                          target: /samba
                container_name: samba-server

volumes:
        samba:
                        
